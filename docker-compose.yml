version: '3.8'

services:
  web:
    image: unmatched-image
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5133:5000"   # Host:Container
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=192.168.0.72,1433;Database=Unmatched;User Id=sa;Password=123456@A;TrustServerCertificate=true
      - DOTNET_URLS=http://+:5000
