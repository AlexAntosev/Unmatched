@page "/"
@inject NavigationManager NavigationManager
@*
    The heroes ladder is the landing screen, so the root route just forwards to it.
*@

@code {
    protected override void OnInitialized()
        => NavigationManager.NavigateTo(Urls.HeroesStatistics, replace: true);
}
