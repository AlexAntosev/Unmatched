@page "/tournaments/{id}"
@using Unmatched.Dtos
@using Unmatched.Dtos.Match
@using Unmatched.Enums
@using Unmatched.Extensions
@using Unmatched.Services.Contracts
@inject ITournamentService TournamentService
@inject IMatchService MatchService
@inject NavigationManager NavManager
@*
    Not in the design pack. A championship shows one panel per stage; a league is just its matches,
    so it reuses the shared 3b list. Planned matches link through to the Add match form.
*@

<PageHeader Breadcrumbs="Crumbs"/>

<div class="um-page">
    @if (_tournament.Type == TournamentType.Championship)
    {
        <div class="bracket">
            @foreach (var stage in Stages())
            {
                var stageMatches = _matches.Where(m => m.Stage == stage).ToList();
                <Panel Title="@stage.GetStageName()" Meta="@($"{stageMatches.Count}")" Class="bracket-stage">
                    @if (stageMatches.Count == 0)
                    {
                        @if (_tournament.CurrentStage + 1 == stage)
                        {
                            <UmButton Size="sm" Icon="bi-magic" OnClick="GenerateMatchesAsync">Generate matches</UmButton>
                        }
                        else
                        {
                            <div class="bracket-empty">Not played yet.</div>
                        }
                    }
                    @foreach (var match in stageMatches)
                    {
                        <div class="bracket-match @(match.IsPlanned ? "is-planned" : null)"
                             @onclick="@(() => OpenMatch(match))">
                            @foreach (var fighter in match.Fighters)
                            {
                                <div class="bracket-fighter">
                                    <FighterChip ImageUrl="@fighter.HeroImageUrl" PlayerAvatarUrl="@fighter.PlayerImageUrl"
                                                 IsWinner="@(match.IsPlanned || fighter.IsWinner)" Size="26"
                                                 Title="@fighter.Hero?.Name"/>
                                    <span class="bracket-name">@fighter.Hero?.Name</span>
                                    <span class="bracket-hp">@(match.IsPlanned ? "–" : fighter.HpLeft?.ToString() ?? "–")</span>
                                </div>
                            }
                            <div class="bracket-meta">
                                @(match.Map?.DisplayName ?? "No map") · @match.Date.ToString("dd MMM yyyy")
                            </div>
                        </div>
                    }
                </Panel>
            }
        </div>
    }
    else
    {
        <MatchList Matches="_leagueMatches" Title="Matches" PageSize="10"
                   Meta="@($"{_leagueMatches.Count} matches")"/>
    }
</div>

@code {
    [Parameter]
    public string Id { get; set; } = string.Empty;

    private TournamentDto _tournament = new();
    private List<UiMatchDto> _matches = new();
    private List<UiMatchLogDto> _leagueMatches = new();

    [Inject]
    protected BlazorBootstrap.PreloadService PreloadService { get; set; } = default!;

    private IReadOnlyList<Breadcrumb.Crumb> Crumbs =>
    [
        new("Tournaments", Urls.Tournaments), new(_tournament.Name ?? string.Empty)
    ];

    private IEnumerable<Stage> Stages()
    {
        for (var stage = _tournament.InitialStage; stage <= Stage.GrandFinals; stage++)
        {
            yield return stage;
        }
    }

    protected override async Task OnParametersSetAsync()
    {
        if (!Guid.TryParse(Id, out var tournamentId) || tournamentId == Guid.Empty)
        {
            return;
        }

        try
        {
            PreloadService.Show();

            _tournament = await TournamentService.GetAsync(tournamentId);
            _matches = (await MatchService.GetByTournamentIdAsync(tournamentId)).ToList();

            _leagueMatches = (await MatchService.GetMatchLogAsync())
                .Where(m => m.TournamentName == _tournament.Name)
                .OrderByDescending(m => m.Date)
                .ToList();
        }
        finally
        {
            PreloadService.Hide();
        }
    }

    private async Task GenerateMatchesAsync()
    {
        await TournamentService.GenerateMatchesAsync(_tournament.Id);
        await OnParametersSetAsync();
    }

    /// <summary>Only a planned match is worth opening - it is the one still waiting for a result.</summary>
    private void OpenMatch(UiMatchDto match)
    {
        if (match.IsPlanned)
        {
            NavManager.NavigateTo($"{Urls.Match}/{match.Id}");
        }
    }
}
